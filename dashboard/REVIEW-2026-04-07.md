# Dashboard Review - 2026-04-07

## ✅ Findings & Fixes

### FIXED Issues
1. **Route Matching Bug (T-035)** 
   - `/api/stats/history` returned HTML instead of JSON
   - Root cause: `/api/stats` check came BEFORE `/api/stats/history` in server.cjs
   - Fixed: Reordered route checks (history check now comes first)
   - Status: ✅ Working

---

## ⚠️ Pending Features (Needs Implementation)

### High Priority Issues
| Task | Feature | Status | Notes |
|------|---------|--------|-------|
| T-026 | Search + Filter (TaskQueue) | pending | Missing search box, tag/priority filters, URL state |
| T-027 | Deadline Warnings | pending | No deadline badges, no red/yellow highlighting |
| T-029 | Telegram Notifications | pending | No notifications API endpoints |

### Medium Priority Issues
| Task | Feature | Status | Notes |
|------|---------|--------|-------|
| T-016 | Settings Page | pending | Read-only display only, no editable fields |
| T-028 | Archive/Hide Completed | pending | No toggle to hide done tasks, no archive tab |
| T-031 | Auto Git Commit | pending | TASKS.json changes not auto-committed |
| T-033 | PM2 Process Manager | pending | Not configured, no crash recovery |
| T-034 | Swap Memory API | pending | No `/api/stats/swap` endpoint |

### Low Priority Issues
| Task | Feature | Status | Notes |
|------|---------|--------|-------|
| T-011 | Home Assistant Panel | pending | FreeRide skill exists but HA panel UI missing |
| T-019 | PWA Support | pending | No manifest.json, no service worker |
| T-030 | Weekly Summary | pending | Cron scheduled but no dashboard display |

---

## 🔍 Test Results

### API Endpoints
- ✅ `/api/stats` — working
- ✅ `/api/stats/history` — fixed & working
- ✅ `/api/tasks` — working
- ✅ `/api/activity` — working
- ✅ `/api/logs` — working
- ✅ `/api/memory` — working
- ✅ `/api/skills/freeride/status` — working
- ❌ `/api/notifications` — not found
- ❌ `/api/alerts` — not found

### Components
- ✅ Overview (stats display)
- ✅ TaskQueue (CRUD operations, drag-drop)
- ✅ Sidebar (navigation)
- ✅ Agents (activity display)
- ✅ Logs (log streaming)
- ✅ Memory (memory files)
- ✅ FreeRide (skill integration)
- ⚠️  Settings (read-only)

### Build
- ⚠️ Chunk size warning (595KB JS) — consider code splitting
- ✅ Build completes successfully

---

## 📋 Recommendations

1. **Quick Wins** (Easy to implement)
   - [ ] Add filter UI to TaskQueue (search + tag/priority filters)
   - [ ] Add deadline badge styling
   - [ ] Add "Hide Completed" toggle with localStorage

2. **Medium Effort**
   - [ ] Implement `/api/notifications` and `/api/alerts` endpoints
   - [ ] Create Settings component with editable fields
   - [ ] Add swap memory to stats API

3. **Infrastructure**
   - [ ] Setup PM2 for process management
   - [ ] Create git commit hook for TASKS.json changes
   - [ ] Add PWA manifest + service worker

---

**Dashboard Status:** 70% complete
**Server Status:** Healthy (API routes fixed)
**Next Steps:** Address high-priority pending features (T-026, T-027, T-029)
